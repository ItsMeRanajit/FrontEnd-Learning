prompt("hey im prompt");
// prompt is similar like alert but it also takes some input and that input can be taken in any variable
//prompt is the way to take input in js(not sure)

// prompt cannot be run on node.js it must be run on browser

let clg = prompt("enter your college name")
console.log("your college name is ", clg)

// whatever is the input of prompt it takes the input as strings.
//so dont use stricter version of comparisn operator